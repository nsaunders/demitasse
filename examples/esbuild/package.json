{
  "private": true,
  "scripts": {
    "build": "node build",
    "dev": "node build --serve"
  },
  "devDependencies": {
    "demitasse": "^0.5.0",
    "esbuild": "^0.12.15",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  }
}
